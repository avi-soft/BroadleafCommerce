spring:
  cloud:
    config:
      uri: https://localhost:8888/config
      username: configClient
      fail-fast: true
      retry:
        max-attempts: 50
        max-interval: 10000
      name: browse
      enabled: true
    bus:
      enabled: true